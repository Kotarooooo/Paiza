<?php
    // 自分の得意な言語で
    // Let's チャレンジ！！
    $totalMerchandise = trim(fgets(STDIN));
    for($i = 0; $i < $totalMerchandise; $i++) {
        $dates = explode(" ", trim(fgets(STDIN)));
        if ($dates == 0) {
            $foodPrice = array_sum($date[1]);
        }
     
    }
            
    
?>